{
  "rules": [],
  "rule_notes": {},
  "note": "Rule IDs added here are intentionally suppressed pending follow-up. Each entry must include why and a date in rule_notes. All rules previously suppressed pending PR-1 are now genuinely fixed: color-contrast (sticky surfaces are now opaque) and scrollable-region-focusable (mobile catalog uses cards now, not horizontal-scroll table) were resolved in the chrome work; aria-required-parent is now resolved by dropping the unsupported role=row/columnheader scaffold from MarginTable (the div-grid is virtualized via react-window, so a real <table> conversion is infeasible). Sort state is communicated via the sort buttons' visible arrow glyph and `title` attribute (e.g. 'Sort Brand descending'); aria-sort cannot be retained without the columnheader role host."
}
