# DEPRECATED — see AGENTS.md in repo root.
#
# Cursor, Antigravity, and Claude Code all read AGENTS.md now.
# GEMINI.md layers Antigravity-specific overrides on top.
# This file is kept only so legacy tooling doesn't error on a missing
# path; it intentionally contains no rules of its own. Do not add rules
# here — edit AGENTS.md instead.
#
# Pointers:
#   AGENTS.md           — canonical agent rules (all agents)
#   GEMINI.md           — Antigravity-only overrides
#   docs/AI-CONTEXT.md  — canonical project context
#   docs/ROADMAP.md     — phase plan
