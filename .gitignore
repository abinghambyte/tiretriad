# UI audit - local Playwright artifacts (never commit)
docs/audit-screenshots/
docs/audit-storage/

# Playwright runtime output (snapshots ARE tracked; these are not)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
.vercel
dist-ssr
*.local
.env
.env.*.local
# Never commit Cloud Functions secrets (explicit; `.env` also matches nested paths)
functions/.env

# Local mkcert / dev TLS (see docs/AI-CONTEXT.md)
.certs/

# Editor directories and files
.cursor/*
!.cursor/rules/
!.cursor/rules/**
!.cursor/environment.json
!.cursor/settings.json
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
.env*.local

# Productivity plugin (local-only; not tracked)
/TASKS.md
/CLAUDE.md
/dashboard.html
/memory/
/.claude/

# Session handoffs: local scratch space, not tracked
/_handoffs/

.superpowers/
